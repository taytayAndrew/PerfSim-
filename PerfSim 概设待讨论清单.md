# PerfSim 概设待讨论清单

> 文档目的：记录概设推进过程中需要逐步讨论和决策的问题，按优先级排列。
> 已对齐的结论直接标记 ✅，待讨论的标记 🔲，讨论中标记 💬。

---

## 已对齐结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 架构版本 | ✅ 采用**整体架构概设**（2层：Chrome Extension + 本地 Node.js Server） |
| 2 | 4层架构（Task Service / MCP Bridge）| ✅ 暂不在当前版本范围内 |
| 3 | 脚本注入方式 | ✅ 插入式**预置通用脚本**，运行时注入录制数据作为参数，模拟优化后页面真实表现 |
| 4 | 脚本生成方式 | ✅ 脚本**预先写好**（非自动生成），一个优化策略对应一个脚本文件，可插拔 |
| 5 | 两阶段执行模型 | ✅ 第一阶段录制（CDP 拦截所有响应 + 识别串行链），第二阶段注入脚本重放 |
| 6 | 缓存数据范围 | ✅ 只缓存**串行链上的接口响应**（完整数据，不截断），响应体 > 500KB 的接口不缓存走真实网络并标记 |
| 7 | Service Worker 处理 | ✅ **不禁用 SW**，录制阶段标记响应来源（SW cache / 网络），注入脚本只拦截「来自网络的串行链请求」 |
| 8 | 推演过程 UI | ✅ Popup 全屏遮罩 + 进度条 + 「勿动」警告文案，检测到 Tab 切换则标记结果「可能不准确」并提供重跑选项 |
| 9 | 浏览器控制库 | ✅ 采用 **Puppeteer**（与 Lighthouse 同源，CDP 支持最完整） |
| 10 | 理论推演计算模型 | ✅ 见下方 P1-3 详细说明 |
| 11 | 用户上传 Lighthouse JSON | ✅ **废弃**，Server 录制阶段同时产出 baseline 数据，Extension 直接使用，无需用户手动上传 |
| 12 | 整体交互流程 | ✅ 两阶段分离：**阶段一「分析」**（自动）→ 展示串行链 + 理论预测；**阶段二「推演」**（用户主动触发）→ 展示 before/after 对比 |
| 13 | 串行链展示 | ✅ 展示具体接口 path 列表，截断 host 和 query 参数 |
| 14 | 部分接口排除 | ✅ MVP 不支持，整条链作为整体推演 |
| 15 | 无串行链时的处理 | ✅ 展示「✅ 未发现串行请求链问题」，不展示推演按钮；有串行链但不影响指标时展示警告并保留推演按钮 |
| 16 | 支持指标 | ✅ MVP 支持 **LCP + TTI + FCP**，TBT 后续迭代 |
| 17 | 置信度设计 | ✅ 置信度是**规则维度**的固定值（规则作者预置），写在规则定义里，展示在分析结果中，不在规则选择列表里展示 |
| 18 | 多规则录制策略 | ✅ 一次录制，多规则共用同一份数据，规则引擎并行分析 |
| 19 | 规则选择列表 | ✅ MVP 就展示规则选择列表（即使只有一条规则），建立「可插拔工具」的用户心智，列表底部加「更多规则即将推出」 |
| 20 | 报告导出 | ✅ MVP 支持导出 **HTML 格式**，内联样式，双击可直接打开，可分享 |
| 21 | 历史记录 | ✅ 按 URL 分组，每个 URL 保留最近 1 条分析记录，存 IndexedDB |
| 22 | Server 启动引导 | ✅ 写在 README，用户定位是前端开发者和测试工程师 |
| 23 | 规则架构定位 | ✅ 官方提供内置规则 + 脚本模板，用户可本地编辑器写自定义规则，导入 .js 文件使用 |
| 24 | 规则三件事契约 | ✅ 每条规则必须实现：`analyze`（分析录制数据）、`calculateTheoretical`（计算理论收益）、`buildScript`（生成注入脚本字符串） |
| 25 | 脚本注入方式 | ✅ 参数直接内联进闭包（替换占位符），不使用全局变量，避免被页面覆盖 |
| 26 | 脚本 DOM 操作约定 | ✅ 网络拦截类规则禁止修改 DOM；资源预加载类规则允许插入 `<link rel="preload">`；其他 DOM 操作禁止 |
| 27 | 规则支持边界 | ✅ 支持：网络请求类优化（串行链、慢接口、预请求、缓存）+ 关键资源预加载；不支持：服务端类（SSR/骨架屏）、HTML 结构类、导航层 Redirect |
| 28 | 多条串行链 | ✅ 一次录制分析所有串行链，推演时同时模拟所有链并行化，理论收益叠加计算 |
| 29 | 会话管理 | ✅ 分析和推演通过 session id 关联临时文件，用户重新分析时旧文件自动清理 |
| 30 | Popup 尺寸 | ✅ 推演报告单独开新 Tab 页展示，Popup 只展示规则选择和分析摘要 |
| 31 | 目标用户 | ✅ 前端工程师（探索式，评估优化收益）+ 产品经理（看结论层，判断前端/后端责任边界） |
| 32 | 报告结构 | ✅ 顶部结论摘要层（给产品）+ 技术细节折叠（给工程师），一份报告分层展示，HTML 导出直接分享 |
| 33 | 分析触发方式 | ✅ 读当前 Tab 的 URL + 自动透传 Cookie，用户无感知，无需手动输入 URL |
| 34 | 产品定位 | ✅ 明确为**首屏冷启动**性能分析，Puppeteer 打开全新 Chrome 实例，与 LCP/FCP/TTI 指标语义一致 |
| 35 | 串行链识别阈值 | ✅ 由规则脚本的 `analyze()` 自己决定，系统不硬编码阈值；结果页支持用户按深度/耗时过滤 |
| 36 | analyze() 返回结构 | ✅ 标准化契约：`{ severity, affectsLCP, chains: [{ depth, totalDelayMs, urls, confirmed }], summary }` |
| 37 | 规则执行位置 | ✅ Server 端执行（analyze / calculateTheoretical / buildScript 全在 Node.js 里跑），Extension 只负责 UI 和触发 |
| 38 | 自定义规则导入 | ✅ Extension UI 上传 .js 文件 → POST /api/rules/import → Server 验证契约 → 保存 → 立即出现在规则列表 |
| 39 | 规则安全边界 | ✅ MVP 不做沙箱隔离，文档注明「自定义规则运行在本地 Node.js 环境，请只导入你信任的脚本」 |
| 40 | 报告数据流 | ✅ Server → SSE → Content Script → Background SW → IndexedDB → Report Tab 读取渲染 |
| 41 | SSE 连接维持方 | ✅ Content Script 负责（注入在目标页面，只要页面不关就存活，最稳定） |
| 42 | 遮罩位置 | ✅ Content Script 注入半透明遮罩到目标页面 + Popup 同步进度条；Puppeteer 是 headless 用户不可见，遮罩不影响测量 |
| 43 | 失败处理 | ✅ Server 推送 SSE `event: error` → Content Script 移除遮罩 → Popup 展示人话错误原因 + 重试按钮 |
| 44 | MVP 内置规则数量 | ✅ 至少 2 条：① 串行请求链并行化；② 关键资源预加载（`<link rel="preload">`，影响 FCP/LCP） |
| 45 | 报告输出内容 | ✅ 只展示优化方向和量化收益（能提升多少 ms），不告诉用户怎么改代码，具体实现由开发者自行决定 |

---

## 第一优先级（动工前必须对齐）

### ✅ P1-1：Puppeteer vs Playwright？
- **决策**：采用 **Puppeteer**，与整体架构概设一致

---

### ✅ P1-2：脚本注入的具体实现逻辑（核心难点）
- **已对齐方案**：两阶段「录制 + 重放」
  1. **录制阶段**：Puppeteer 正常跑一遍页面，CDP 拦截所有请求响应，记录 URL + Response + 时序，识别串行链
  2. **注入阶段**：将录制数据序列化为脚本参数，通过 `evaluateOnNewDocument` 注入通用脚本，命中串行链的请求直接走缓存返回（延迟趋近 0），其余请求走真实网络
  3. **重放阶段**：注入脚本后跑 Lighthouse，得到模拟后的真实指标
- **脚本参数结构**：`CACHE`（响应缓存）、`CHAIN_URLS`（串行链 URL 列表）、`STRATEGY`（策略配置）
- **两个核心技术难点**（需要专项设计）：
  - **URL 归一化**：去除时间戳/随机参数，识别同一接口的不同请求
  - **Service Worker 拦截优先级**：需验证各 Chrome 版本中 `evaluateOnNewDocument` 与 SW fetch event 的执行顺序稳定性
- **产品侧注意**：推演结果是「优化潜力上限」（串行链接口延迟趋近 0 的理论值），非并行化后的精确预测，UI 上需明确传达

---

### ✅ P1-3：理论推演的计算模型
- **数据来源**：Lighthouse JSON 中的 `network-requests` audit，提取每个请求的 startTime、endTime、duration
- **计算逻辑**：
  ```
  理论可节省时间 = 串行链中第2、3...N个请求的 startTime 延迟之和
  （并行化后这些请求可以和第1个同时发出）

  示例：A(0→200ms) → B(200→350ms) → C(350→450ms)
    串行总耗时：450ms
    并行理论耗时：max(200, 150, 100) = 200ms
    理论可节省：250ms
  ```
- **LCP 关联判断**：
  - `串行链结束时间 ≤ LCP renderTime` → 时序关联，给出 LCP 预测收益（置信度：中）
  - `串行链结束时间 > LCP renderTime` → LCP 不在关键路径上，LCP 预测收益 = 0，展示「该串行链不影响 LCP」
- **MVP 只展示 LCP 指标**，TTI / TBT 等指标后续迭代再加
- **置信度标注**：基于时序关联（非因果验证），标注「中」，引导用户通过真实推演验证
- **展示形式**：单一数值 + 置信度标注，例：`LCP 预测：3200ms → 2950ms（置信度：中）`

---

## 第二优先级（开发中需要决策）

### 🔲 P2-1：登录态 / Cookie 方案
- **背景**：真实推演时 Server 用 Puppeteer 打开页面，如果目标页面需要登录怎么办
- **待明确**：
  1. MVP 阶段是否只支持**无需登录的公开页面**？
  2. 若需要支持登录态：Cookie 如何获取？（手动复制？Extension 自动提取当前页面 Cookie？）
  3. Cookie 过期处理机制？
- **决策**：待讨论

---

### 🔲 P2-2：端口配置
- **背景**：文档写死 `localhost:3000`
- **待明确**：
  1. 端口是否支持用户自定义？
  2. 端口冲突时的处理方案？
  3. Extension 如何感知 Server 端口变化？
- **建议**：默认 3000，支持 Extension 配置页修改，Server 读取配置启动
- **决策**：待讨论

---

### 🔲 P2-3：Lighthouse + Puppeteer 版本适配
- **背景**：Lighthouse v10+ 已不推荐作为 Puppeteer 直接插件使用，共享 Chrome 实例方式有变化
- **待明确**：
  1. 确认目标 Lighthouse 版本
  2. 集成方案（`lighthouse` npm 包 + CDP port 方式 or `@lhci/cli`）
- **决策**：待讨论

---

### 🔲 P2-4：Extension ID 与 CORS
- **背景**：开发态 Extension ID 是临时的，每人不同；发布后才固定
- **待明确**：
  1. 本地开发阶段如何处理 CORS？
  2. 是否在 Server 启动时提供配置界面？
- **决策**：待讨论

---

### 🔲 P2-5：并发控制与任务队列
- **背景**：用户重复点击「推演」会启动多个 Puppeteer 实例，互相抢占 CPU 导致 Lighthouse 数据失真
- **待明确**：
  1. 同时只允许一个推演任务，后续请求排队还是直接拒绝并提示？
  2. 任务队列的状态如何同步给 Extension UI？
- **建议**：单任务模型，重复触发时提示「推演进行中，请等待完成」
- **决策**：待讨论

---

### 🔲 P2-6：两阶段状态持久化
- **背景**：录制阶段数据若只存内存，Server 意外重启后数据丢失，用户不知道发生了什么
- **待明确**：
  1. 录制数据存内存还是落磁盘临时文件？
  2. 临时文件的清理策略（推演完成后立即删除 or 保留 N 分钟）？
- **建议**：落磁盘临时文件，推演完成或失败后自动清理
- **决策**：待讨论

---

### 🔲 P2-7：Puppeteer 进程泄漏防护
- **背景**：推演进行中用户关闭 Extension 面板 → SSE 断开 → Server 感知不到 → Chrome 进程残留后台
- **待明确**：
  1. SSE 连接断开时如何触发 Server 侧清理？
  2. 是否需要定时检测孤儿 Chrome 进程并强制终止？
- **建议**：SSE 断开事件 → 立即终止对应 Puppeteer 实例并清理临时文件
- **决策**：待讨论

---

### 🔲 P2-8：CSP 检测与提示
- **背景**：目标页面若有严格 CSP，`evaluateOnNewDocument` 注入的脚本会被静默阻断，推演结果与 baseline 相同但不报错
- **待明确**：
  1. 录制阶段解析响应头检测 CSP，发现问题如何提示用户？
  2. 是否尝试绕过（通过 Puppeteer 的 `setBypassCSP`）？
- **建议**：自动调用 `page.setBypassCSP(true)`（Puppeteer 支持），并在报告中注明
- **决策**：待讨论

---

### 🔲 P2-9：Web Worker 内请求无法拦截
- **背景**：注入脚本只能拦截主线程 fetch，Web Worker 内的请求无法拦截，若串行链上有 Worker 请求则推演不完整
- **待明确**：
  1. 录制阶段如何标记来自 Worker 的请求？
  2. 推演报告中如何注明「以下请求无法模拟」？
- **决策**：待讨论

---

### 🔲 P2-10：Extension Service Worker 生命周期保活
- **背景**：Chrome MV3 的 Extension SW 约 30 秒无活动会被终止，而 Lighthouse 推演通常需要 45-60 秒，导致 SSE 回调无人接收，UI 永久卡在「推演中」
- **待明确**：
  1. 推演期间通过 `chrome.alarms` 每 20 秒唤醒一次 SW？
  2. 还是将 SSE 监听放在 content script 或 offscreen document？
- **建议**：使用 `chrome.alarms` 心跳保活，是 MV3 官方推荐方式
- **决策**：待讨论

---

### 🔲 P2-11：Windows 环境兼容
- **背景**：目标开发环境为 Windows，Puppeteer 在 Windows 上有若干已知问题
- **需处理**：
  1. Chrome 可执行文件路径含空格（`Program Files`）需特殊处理
  2. 所有文件路径统一使用 `path.join`，避免反斜杠问题
  3. 进程终止使用 `taskkill` 而非 `SIGTERM`
- **决策**：开发时作为规范落地，不需要单独讨论

---

## 第三优先级（MVP 后迭代）

### 🔲 P3-1：基线数据采集体验优化
- **背景**：当前方案需用户手动跑 Lighthouse → 导出 JSON → 上传 Extension，体验差
- **方向**：Extension 直接触发 Lighthouse 跑，无需手动操作
- **决策**：MVP 后迭代

---

### 🔲 P3-2：用户心智模型——理论推演 vs 真实推演
- **背景**：两种模式差异需在 UI 上清晰传达，「优化潜力上限」的概念需要用户易于理解
- **待明确**：文案策略、视觉区分方式
- **决策**：待讨论（产品/设计介入）

---

### 🔲 P3-3：首期规则覆盖面
- **背景**：MVP 只有「串行请求链深度 > 3」一条规则
- **待明确**：
  1. 第二批规则候选列表（慢请求识别、请求数过多、资源包过大等）
  2. 规则扩展的优先级排序
- **决策**：待讨论

---

## 讨论记录

| 日期 | 问题编号 | 讨论结论 |
|---|---|---|
| 2026-05-13 | 架构版本 | ✅ 采用 2 层整体架构概设 |
| 2026-05-13 | 脚本方式 | ✅ 插入式预置通用脚本，运行时注入录制数据参数 |
| 2026-05-13 | 执行模型 | ✅ 两阶段录制+重放，只缓存串行链上的接口响应（完整数据） |
| 2026-05-13 | SW 处理 | ✅ 不禁用 SW，录制阶段标记响应来源，注入脚本只拦截来自网络的串行链请求 |
| 2026-05-13 | 推演 UI  | ✅ 全屏遮罩 + 勿动警告 + Tab 切换检测标记结果 |
| 2026-05-13 | P1-1 浏览器库 | ✅ 采用 Puppeteer |
| 2026-05-18 | P1-3 理论推演 | ✅ 时序关联判断 + 单一数值 + 置信度标注，MVP 支持 LCP+TTI+FCP |
| 2026-05-18 | 交互流程 | ✅ 废弃手动上传 JSON，两阶段分离（分析/推演），规则选择列表从 MVP 开始展示 |
| 2026-05-18 | 置信度 | ✅ 规则维度固定值，写在规则定义里，不在规则列表里展示 |
| 2026-05-20 | P2 风险一 | ✅ HTTP/2 假串行：检测 B 的参数是否含 A 的响应值，保守策略宁可误判为真串行 |
| 2026-05-20 | P2 风险二 | ✅ POST/GraphQL：缓存 key = URL + method + hash(requestBody)；串行链结果向用户展示清单，分「确认串行」和「疑似串行」，用户自行判断 |
| 2026-05-20 | P2 风险三 | ✅ 动态 Token：监听页面 URL 变化，跳转到登录页立即中断并提示「Cookie 可能失效」 |
| 2026-05-20 | P2 风险四 | ✅ Lighthouse 方差：baseline 和 simulation 各跑 3 次取中位数，共 6 次，提前告知用户预计耗时 |
| 2026-05-20 | 脚本规范 | ✅ 参数内联进闭包，DOM 操作有限制，支持边界明确（网络请求类） |
| 2026-05-20 | 历史记录 | ✅ 按 URL 分组，每个 URL 保留最近 1 条，存 IndexedDB |
| 2026-05-20 | Popup 布局 | ✅ 报告单独开新 Tab，Popup 只展示规则选择和分析摘要 |
| 2026-05-20 | 目标用户 | ✅ 前端工程师（评估优化收益）+ 产品经理（判断前端/后端责任边界） |
| 2026-05-20 | 报告结构 | ✅ 顶部结论摘要层 + 技术细节折叠，一份报告分层展示 |
| 2026-05-20 | 产品定位 | ✅ 首屏冷启动性能分析，Puppeteer headless 独立实例 |
| 2026-05-20 | 分析触发 | ✅ 读当前 Tab URL + 自动透传 Cookie，用户无感知 |
| 2026-05-20 | 串行链阈值 | ✅ 规则脚本自己决定，结果页支持过滤，系统不硬编码 |
| 2026-05-20 | analyze 契约 | ✅ 返回结构标准化：severity / affectsLCP / chains / summary |
| 2026-05-20 | 规则执行位置 | ✅ 全部在 Server 端 Node.js 执行，Extension 只做 UI |
| 2026-05-20 | 自定义规则导入 | ✅ Extension UI 上传 → POST /api/rules/import → Server 验证保存 |
| 2026-05-20 | 规则安全 | ✅ MVP 不做沙箱，文档注明信任声明 |
| 2026-05-20 | 报告数据流 | ✅ SSE → Content Script → Background SW → IndexedDB → Report Tab |
| 2026-05-20 | SSE 维持方 | ✅ Content Script（目标页面最稳定存活单元） |
| 2026-05-20 | 遮罩 | ✅ Content Script 注入目标页面 + Popup 进度条，不影响 Lighthouse 测量 |
| 2026-05-20 | 失败处理 | ✅ SSE event:error → 移除遮罩 → 人话错误 + 重试按钮 |
| 2026-05-20 | MVP 规则数量 | ✅ 至少 2 条：串行链并行化 + 关键资源预加载 |
| 2026-05-20 | 报告输出 | ✅ 只给优化方向和收益数字，不告诉用户怎么改代码 |
